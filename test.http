### baseUrl 변수 — 나중에 배포 주소로 바꿀 때 이 한 줄만 수정하면 돼요!
@baseUrl = http://localhost:3000

### ─────────────────────────────────────────────────
### 실습#1: GET 엔드포인트 (✅ 완료)
### ─────────────────────────────────────────────────

### 전체 할 일 목록 조회
GET {{baseUrl}}/todos

### 단일 항목 조회 (존재하는 id)
GET {{baseUrl}}/todos/1

### 단일 항목 조회 (존재하지 않는 id) → 404 확인
GET {{baseUrl}}/todos/999

### 완료된 항목만 필터링
GET {{baseUrl}}/todos?completed=true

### 완료되지 않은 항목만 필터링
GET {{baseUrl}}/todos?completed=false


### ─────────────────────────────────────────────────
### 실습#2: POST 엔드포인트 (✅ 완료)
### ─────────────────────────────────────────────────

### POST /todos — title 없이 요청 → 400 확인
POST {{baseUrl}}/todos
Content-Type: application/json

{}

### POST /todos — title 포함해서 요청 → 201 + 새 항목 확인
POST {{baseUrl}}/todos
Content-Type: application/json

{
  "title": "새 할 일 추가하기"
}


### ─────────────────────────────────────────────────
### 실습#3: PATCH & DELETE 테스트
### ─────────────────────────────────────────────────

### TODO 1: PATCH /todos/:id — completed: true 로 수정
###   힌트: PATCH {{baseUrl}}/todos/1 + body: { "completed": true }


### TODO 2: PATCH /todos/:id — title 도 수정
###   힌트: body 에 { "title": "수정된 제목" }


### TODO 3: DELETE /todos/:id — 항목 삭제
###   힌트: DELETE {{baseUrl}}/todos/1


### TODO 4: 존재하지 않는 id로 PATCH/DELETE → 404 확인
###   예: PATCH/DELETE /todos/999

